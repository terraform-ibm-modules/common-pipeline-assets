name: Release-Pipeline
# Trigger on push(merge) to main branch
on:
  push:
    branches:
      - main

jobs:
  call-release-pipeline:
#    do not merge this update to point to @main
    uses: terraform-ibm-modules/common-pipeline-assets/.github/workflows/common-release.yml@remove_ci_image
    secrets: inherit
    with:
      sign: false
